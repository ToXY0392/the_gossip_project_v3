<!DOCTYPE html>
<html data-bs-theme="dark">
  <head>
    <title>TheGossipProject</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body class="min-vh-100 d-flex flex-column">
    <header class="border-bottom border-secondary-subtle">
      <div class="container py-4">
        <div class="d-flex flex-column flex-lg-row justify-content-between align-items-lg-end gap-3">
          <div>
            <p class="gossip-kicker text-uppercase mb-1">Edition de Nuit</p>
            <%= link_to "The Gossip Ledger", root_path, class: "navbar-brand gossip-brand fs-1 lh-1 m-0 p-0 d-inline-block" %>
            <p class="text-body-secondary mb-0">Le journal des rumeurs et indiscretions.</p>
          </div>
          <nav class="nav nav-pills gap-2">
            <%= link_to "Home", root_path, class: "nav-link px-3 py-2 #{'active' if current_page?(root_path)}" %>
            <%= link_to "Team", team_path, class: "nav-link px-3 py-2 #{'active' if current_page?(team_path)}" %>
            <%= link_to "Contact", contact_path, class: "nav-link px-3 py-2 #{'active' if current_page?(contact_path)}" %>
          </nav>
        </div>
      </div>
    </header>

    <main class="container py-4 flex-grow-1">
      <%= yield %>
    </main>

    <footer class="mt-auto border-top border-secondary-subtle">
      <div class="container py-3 d-flex justify-content-between align-items-center flex-wrap gap-2">
        <small class="text-body-secondary">The Gossip Ledger</small>
        <small class="badge rounded-pill text-bg-dark border border-secondary-subtle">No JS Edition</small>
      </div>
    </footer>
  </body>
</html>
