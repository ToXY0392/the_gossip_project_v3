<!DOCTYPE html>
<html data-bs-theme="dark">
  <head>
    <title>TheGossipProject</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body class="min-vh-100 d-flex flex-column">
    <header class="border-bottom border-secondary-subtle">
      <div class="container py-4">
        <div class="d-flex flex-column flex-lg-row justify-content-between align-items-lg-end gap-3">
          <div>
            <%= link_to "THP Gossip Project", root_path, class: "navbar-brand gossip-brand fs-1 lh-1 m-0 p-0 d-inline-block" %>
            <p class="text-body-secondary mb-0">Vos petits potins THP pour ne pas finir pirate.</p>
          </div>
          <nav class="nav nav-pills gap-2">
            <%= link_to "Team", team_path,
                        class: "nav-link px-3 py-2 #{'active' if current_page?(team_path)}" %>
            
            <%= link_to "Contact", contact_path,
                        class: "nav-link px-3 py-2 #{'active' if current_page?(contact_path)}" %>

            <% if current_user %>
              <div class="card user-nav-card">
                <div class="card-body p-2 d-flex flex-column align-items-start gap-1">
                  <span class="text-body-secondary mb-0">Bonjour, <%= current_user.first_name %> !</span>
                  <div class="d-flex align-items-center gap-1">
                    <%= button_to "Logout",
                                  logout_path,
                                  method: :delete,
                                  form: { class: "d-inline-flex align-items-center mb-0" },
                                  class: "btn btn-outline-light btn-sm py-0 px-2" %>
                    <%= link_to user_path(current_user),
                                class: "btn btn-outline-light btn-sm py-0 px-2 d-inline-flex align-items-center justify-content-center",
                                title: "Paramètres" do %>
                      <i class="bi bi-gear"></i>
                    <% end %>
                  </div>
                </div>
              </div>
            <% else %>
              <%= link_to "Login", login_path,
                          class: "nav-link nav-link-primary px-3 py-2 #{'active' if current_page?(login_path)}" %>
            <% end %>
          </nav>
        </div>
      </div>
    </header>

    <main class="container py-4 flex-grow-1">
      <%= yield %>

      <div class="mt-4 pt-3 text-center">
        <%= link_to "Home", root_path, class: "btn btn-home-footer" %>
      </div>
    </main>

    <footer class="mt-auto border-top border-secondary-subtle">
      <div class="container py-3 d-flex justify-content-between align-items-center flex-wrap gap-2">
        <small class="text-body-secondary">The Gossip Project</small>

        <small class="text-body-secondary d-flex align-items-center gap-2">
          Contact us :
          <a href="https://www.instagram.com" class="link-light link-offset-2 d-inline-flex align-items-center gap-1" target="_blank" rel="noopener">
            <i class="bi bi-instagram"></i>
            <span class="d-none d-sm-inline">Instagram</span>
          </a>
          <a href="https://www.linkedin.com" class="link-light link-offset-2 d-inline-flex align-items-center gap-1" target="_blank" rel="noopener">
            <i class="bi bi-linkedin"></i>
            <span class="d-none d-sm-inline">LinkedIn</span>
          </a>
          <a href="https://www.thehackingproject.org" class="link-light link-offset-2 d-inline-flex align-items-center gap-1" target="_blank" rel="noopener">
            <i class="bi bi-globe2"></i>
            <span class="d-none d-sm-inline">THP</span>
          </a>
        </small>
      </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
